# 三个 Skill 现状检查报告

**检查时间**: 2026-03-24  
**检查范围**: S5 (parse-requirement), S1 (search-history-cases), S2 (assessment-reasoning)

---

## 📊 总体进度概览

| Skill | 核心功能 | 数据库集成 | 配置完整性 | 生产就绪度 |
|-------|---------|-----------|-----------|-----------|
| S5 需求解析 | ✅ 完成 | ⚠️ 部分 (枚举已同步) | ⚠️ 中等 | 🟡 70% |
| S1 历史检索 | ✅ 完成 | ✅ 完成 | ✅ 高 | 🟢 90% |
| S2 评估推理 | ✅ 完成 | ✅ 完成 | ⚠️ 中等 | 🟡 75% |

---

## 🔍 S5 - ParseRequirementSkill (需求解析)

### ✅ 已完成
- [x] parse/revise/confirm 三阶段交互
- [x] 中英文邮件解析
- [x] 业务类型/服务描述/服务类型识别
- [x] 枚举配置与数据库对齐 (r2-enums.json)
- [x] business_type_inference 自动推断

### ⚠️ 测试阶段/临时方案

| 项目 | 位置 | 问题描述 | 建议 |
|------|------|---------|------|
| **r2-sample-enums.json** | `references/r2-sample-enums.json` | 旧的示例枚举文件 (仅 5 项 service_desc) | 可删除或标记为 deprecated |
| **测试样本文件** | `samples/sample-*.json` | 使用旧枚举编码 (SD001-ST001 等) | 更新为新编码或删除 |
| **文档中的示例** | `SKILL.md:66-302` | 多处引用 `r2-sample-enums.json` | 更新为 `r2-enums.json` |
| **设备型号识别** | 代码逻辑 | 使用正则硬编码，未接入数据库 | 建议从 `job_levels` 或独立表读取 |
| **service_device_models** | 解析输出 | 直接截取原文，未结构化 | 建议解析为结构化列表 |

### 📝 配置问题

```json
// r2-enums.json 中的 model_patterns 还是硬编码
"model_patterns": [
  "[0-9]{1,2}[A-Z][0-9]{2}[A-Z]{1,3}(?:-[A-Z0-9\\.]+)?",
  "MAN\\\\s*B&W-[0-9A-Z\\\\-\\\\.]+",
  "CONSILIUM.*",
  "SG-.*",
  "AUTROSAFETY.*"
]
```
**建议**: 从数据库 `equipment_models` 表动态读取 (如创建该表)

### 🔧 兜底逻辑

| 位置 | 描述 | 影响 |
|------|------|------|
| `business_type: null` | 无法推断业务类型时返回 null | 需要 revise 阶段手动补充 |
| `service_type: null` | 无法识别服务类型 | 需要 revise 阶段手动补充 |
| `confidence: medium` | 默认置信度 | 可能高估识别准确度 |

---

## 🔍 S1 - SearchHistoryCasesSkill (历史检索)

### ✅ 已完成
- [x] PostgreSQL 数据库集成
- [x] pg_trgm 相似度计算
- [x] 自动放宽检索条件 (service_type_relaxed)
- [x] 设备型号命中优先排序
- [x] 时间衰减排序
- [x] 返回 match_reason 可解释性

### ⚠️ 测试阶段/临时方案

| 项目 | 位置 | 问题描述 | 建议 |
|------|------|---------|------|
| **r2-enums-db.json** | `references/r2-enums-db.json` | 从数据库导出的枚举副本 (111KB) | 可删除，直接使用数据库 |
| **input.json** | `input.json` | 默认测试输入文件 | 保留作为示例，但应注明 |
| **npm run search** | `package.json` | 硬编码 `--input ./input.json` | 建议改为提示用户指定输入 |

### 📝 配置问题

```bash
# .env.example 中密码为空
PGPASSWORD=
```
**建议**: 提供 `.env.local` 示例或使用环境变量注入

### 🔧 兜底逻辑

| 位置 | 描述 | 影响 |
|------|------|------|
| `candidate_count: 0` | 无匹配案例时返回空结果 | 正常行为，但 S2 会受影响 |
| `service_type_relaxed: true` | 自动放宽服务类型条件 | 已实现，但可能返回不相关结果 |
| `task_sim_score: 0` | 任务相似度为 0 时仍返回 | 建议设置阈值过滤 |

### 📊 数据库依赖

| 表名 | 必需 | 状态 | 说明 |
|------|------|------|------|
| `evaluation_records` | ✅ | ✅ 已使用 | 主检索表 |
| `evaluation_personnel` | ❌ | ⚠️ 可选 | 人员明细 (当前 S1 未使用) |
| `pg_trgm` 扩展 | ✅ | ✅ 已启用 | 相似度计算 |

---

## 🔍 S2 - AssessmentReasoningSkill (评估推理)

### ✅ 已完成
- [x] 风险识别 (risk_engine)
- [x] 工时估算 (workhour_engine)
- [x] 人力推理 (manpower_engine)
- [x] 数据库模式支持
- [x] 工种职级体系 (v1.1.0)
- [x] 职级覆盖规则 (1146 条)

### ⚠️ 测试阶段/临时方案

| 项目 | 位置 | 问题描述 | 建议 |
|------|------|---------|------|
| **sample.json 文件** | `references/*.sample.json` | 风险/工时/人力规则都是示例数据 | 需要从数据库读取或更新示例 |
| **sample-history-cases.json** | `references/sample-history-cases.json` | 示例历史案例 (1 条) | 保留作为测试，但应注明 |
| **兜底工时** | `workhour_engine.py:54-56` | 无规则时返回 8 小时 | 建议基于服务类型统计 |
| **兜底人数** | `manpower_engine.py:34-40` | 无历史时返回 1 人 | 已实现，但置信度低 |
| **置信度计算** | 各 engine | 基于简单规则 (有无历史/规则) | 建议升级为概率模型 |

### 📝 配置问题

```json
// r5-workhour-rules.sample.json
{
  "rule_id": "WH-001",
  "work_type_code": "WT0001",  // ⚠️ 旧编码，应为 JN*
  "sample_size": 8,            // ⚠️ 硬编码样本数
}
```

```json
// r6-manpower-rules.sample.json
{
  "level_cover_rules": [
    {
      "work_type_name": "轮机工程师",  // ⚠️ 使用名称而非代码
      "higher_level_name": "高级工程师 (T5)"  // ⚠️ 已废弃格式
    }
  ]
}
```

### 🔧 兜底逻辑

| 位置 | 描述 | 代码 | 影响 |
|------|------|------|------|
| **风险识别** | 无规则时返回空列表 | `risk_engine.py` | 正常，但可能漏识别 |
| **工时估算** | 无历史无规则时 8 小时 | `workhour_engine.py:55` | 置信度 low |
| **人力推理** | 无历史时 1 人 | `manpower_engine.py:36` | 置信度 low |
| **置信度汇总** | 取最低置信度 | `main.py:54-67` | 可能过于保守 |

### 📊 数据库依赖

| 表名 | 必需 | 状态 | 说明 |
|------|------|------|------|
| `risk_rules` | ⚠️ 可选 | ✅ 已支持 | 风险规则 |
| `workhour_rules` | ⚠️ 可选 | ✅ 已支持 | 工时规则 |
| `manpower_global_rules` | ⚠️ 可选 | ✅ 已支持 | 全局规则 |
| `manpower_level_cover_rules` | ⚠️ 可选 | ✅ 已更新 | 职级覆盖 (1146 条) |
| `work_types` | ❌ 可选 | ✅ 已创建 | 工种配置 (25 条) |
| `job_levels` | ❌ 可选 | ✅ 已创建 | 职级配置 (193 条) |

### 🐛 已知问题

1. **工时估算过于简化**
   - 仅输出单值，无区间估计
   - 未考虑地区差异、人员技能水平
   - 风险调整仅基于 keyword 匹配

2. **人力推理规则简单**
   - 仅支持串行复用
   - 未考虑时间窗口 (并行任务)
   - 跨工种替代规则未实现

3. **置信度计算粗糙**
   - 仅基于"有无历史/规则"
   - 未考虑样本数量、质量
   - 未考虑规则匹配度

---

## 📋 待办事项优先级

### 🔴 高优先级 (影响生产使用)

1. **S2**: 更新 `*.sample.json` 文件使用新编码体系
2. **S5**: 更新文档中的 `r2-sample-enums.json` 引用
3. **S2**: 实现基于数据库的风险规则 (当前为空)

### 🟡 中优先级 (改进质量)

4. **S2**: 工时估算升级为区间估计
5. **S2**: 人力推理增加时间窗口建模
6. **S5**: 设备型号识别接入数据库
7. **S1**: 删除 `r2-enums-db.json` (直接使用数据库)

### 🟢 低优先级 (优化体验)

8. **S1**: 改进 `npm run search` 交互
9. **S5**: 清理 `r2-sample-enums.json`
10. **所有**: 改进置信度计算逻辑

---

## 📈 生产就绪度评估

### S5 - 需求解析 (70%)
- ✅ 核心解析功能完整
- ✅ 枚举配置已同步数据库
- ⚠️ 文档需要更新
- ⚠️ 设备型号识别需改进
- ⚠️ 置信度评估需优化

### S1 - 历史检索 (90%)
- ✅ 数据库集成完整
- ✅ 检索算法成熟
- ✅ 排序逻辑合理
- ⚠️ 可清理冗余配置文件
- ⚠️ 相似度阈值可配置

### S2 - 评估推理 (75%)
- ✅ 三大引擎功能完整
- ✅ 工种职级体系已建立
- ✅ 数据库表结构已更新
- ⚠️ 规则文件需更新编码
- ⚠️ 兜底逻辑需改进
- ⚠️ 置信度计算需优化

---

## 🎯 下一步建议

### 立即执行
1. 更新 S2 的 `*.sample.json` 使用新编码 (JN*, ET* 等)
2. 更新 S5 文档中的枚举文件引用
3. 测试完整流程 S5 → S1 → S2

### 短期改进 (1-2 周)
4. 实现 S2 风险规则数据库读取
5. 改进工时估算为区间估计
6. 优化置信度计算逻辑

### 长期规划 (1 个月+)
7. 人力推理时间窗口建模
8. 设备型号数据库集成
9. 清理冗余配置文件

---

**总结**: 三个 Skill 核心功能已完成，S1 最接近生产就绪，S5 和 S2 需要文档更新和配置优化。主要技术债务在于示例文件编码不一致和兜底逻辑过于简化。
