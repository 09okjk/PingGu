---
name: generate-report-skill
slug: generate-report-skill
version: 2.0.0
description: 整合已确认需求、历史案例与评估推理结果，生成供工务审核和服贸报价准备使用的结构化评估报告草稿。
changelog: |
  ## [2.0.0] - 双板块结构版
  - 报告结构调整为两大板块：项目信息 + 服务评估
  - 项目信息采用表格形式，包含 11 个字段
  - 服务评估包含：风险提示、施工任务、施工人数及工时、资源需求、综述、审核重点
  - 优化 Markdown 输出格式，提升可读性
  ## [1.0.0] - MVP 初始版本
  - 支持单个 RequirementItem 的结构化评估报告生成
  - 支持风险、任务、总计、工具、耗材、专用工具、设备/备件三栏输出
  - 支持 confidence / source / warnings / review_focus
  - 支持"港口=航修"规则
metadata:
  clawdbot:
    emoji: 📋
    requires:
      bins: ["python3"]
      env: []
    os: ["linux", "darwin", "win32"]
---

# GenerateReportSkill

这是智能评估 Agent 的 S6 报告合成技能。  
用于把 S5 的已确认需求、S1 的历史案例、S2 的风险/工时/人数推理结果，整理成一份结构化评估报告草稿。

该报告的使用顺序为：

1. 工务人员先查看
2. 工务人员与 Agent 对话调整
3. 工务人员审核通过
4. 交给服贸人员用于报价准备

---

## When to Use

✅ 适用于：

- 已经完成 S5 需求确认
- 已经有 S1 历史案例结果
- 已经有 S2 评估推理结果
- 需要生成结构化评估报告草稿
- 需要给工务人员审核
- 需要给服贸人员提供报价依据

---

## When NOT to Use

❌ 不适用于：

- 原始邮件解析（请使用 S5）
- 历史案例检索（请使用 S1）
- 风险 / 工时 / 人数推理（请使用 S2）
- 直接生成报价单
- 直接生成报价项建议
- 多服务项合并总报告

---

## Setup

### 基础用法

```bash
python3 {baseDir}/scripts/main.py \
  --action generate_report \
  --json-input-file "{baseDir}/samples/sample-input.json" \
  --pretty
```

### 快速测试

```bash
cd {baseDir}

python3 scripts/main.py \
  --action generate_report \
  --json-input-file samples/sample-input.json \
  --pretty
```

---

## Options

| 选项 | 类型 | 说明 | 必需 |
|------|------|------|------|
| `--action` | `generate_report` | 执行动作 | ✅ |
| `--json-input-file` | `path` | 输入 JSON 文件路径 | ❌ 与 `--json-input` 二选一 |
| `--json-input` | `json` | 直接传入 JSON 字符串 | ❌ 与 `--json-input-file` 二选一 |
| `--pretty` | `flag` | 格式化输出 JSON | ❌ |

### 输出格式选项

在输入 JSON 的 `options` 中设置 `output_format`：

| 值 | 说明 | 适用场景 |
|----|------|----------|
| `markdown` | 输出 Markdown 格式报告（默认） | 工务人员直接阅读、对话交互 |
| `json` | 输出 JSON 格式 | 程序调用、数据交换 |

**示例**：
```json
{
  "options": {
    "output_format": "markdown",
    "output_language": "zh-CN"
  }
}
```

---

## Input Contract

输入对象结构：

```json
{
  "requirement": {},
  "history_cases": [],
  "assessment_result": {},
  "options": {
    "output_language": "zh-CN",
    "include_source_details": true,
    "include_review_focus": true
  }
}
```

---

## Output Contract

### Markdown 格式（默认，人类可读）

```markdown
# 智能评估报告

## 项目信息

| 字段 | 内容 |
|------|------|
| 业务归口 | 轮机 |
| 服务描述 | 火警系统 |
| 服务类型 | 维修 |
| 服务地点 | 港口 |
| 所属设备名称 | 火警系统 |
| 所属设备型号 | Consilium ALRM CODE 84 |
| 所属设备厂家 | Consilium |
| 服务设备数量 | 1 |
| 服务设备计量单位 | 套 |
| 备注 | Consilium Shanghai 已更换备件但仍有多个报警无法复位 |
| 需求详情 | 经与客户确认：需要自备专用调试电脑 |

## 服务评估

### 风险提示

| 风险等级 | 风险名称 | 说明 | 建议措施 | 置信度 |
|----------|----------|------|----------|--------|
| 🔴 高 | 故障根源未明导致多次登轮 | 客户已自行更换备件但仍有多个报警无法复位 | 建议安排资深电气工程师登轮 | ✓ 高 |

### 施工任务

**任务 1**: 火警系统故障排查

依据历史相似案例整理的施工任务。

### 施工人数及工时

**任务 1**: 火警系统故障排查

| 详细工作内容 | 单位 | 数量 | 工种 | 职级 | 人数 | 单人工时 | 置信度 |
|--------------|------|------|------|------|------|----------|--------|
| - | - | - | 电工 | 技工 (P4) | 1 | 16.0 | ⚡ 低 |
| - | - | - | 电气工程师 | 高级工程师 (T5) | 1 | 16.0 | ⚡ 低 |

### 综述

| 项目 | 数值 | 置信度 |
|------|------|--------|
| 是否航修 | 是 | ✓ 高 |
| 总小时数 | 16 小时 | ⚠ 中 |
| 总人数 | 2 人 | ⚠ 中 |

> 总小时数表示整体施工总时长；总人数表示理论最小所需人数，并非各任务人数直接相加。

### 资源需求

#### 需要工具
| 工具名称 | 工具类型 | 计量单位 | 需要数量 |
|----------|----------|----------|----------|
| - | - | - | - |

#### 耗材
| 耗材名称 | 耗材型号 | 计量单位 | 数量 |
|----------|----------|----------|------|
| - | - | - | - |

#### 专用工具
| 专用工具名称 | 专用工具型号 | 计量单位 | 数量 |
|--------------|--------------|----------|------|
| - | - | - | - |

#### 设备/备件需求

**待确认**
- 设备/备件需求

### 审核重点

#### ⚠️ 警告提示
- 🔴 设备/备件归属缺乏明确依据，建议人工确认。

#### 重点关注
- 请重点确认高风险项是否需要影响工期、资源投入。
```

### JSON 格式（程序调用）

在 `options` 中设置 `"output_format": "json"` 即可输出 JSON 格式。

## 审核重点

### ⚠️ 警告提示
- 🔴 设备/备件归属缺乏明确依据，建议人工确认。

### 重点关注
- 请重点确认高风险项是否需要影响工期、资源投入或现场作业安排。
```

### JSON 格式（程序调用）

在 `options` 中设置 `"output_format": "json"` 即可输出 JSON 格式。

---

## Core Rules

1. **一项一报**
   - 每个需求项生成一份独立评估报告

2. **双板块结构**
   - 板块一：项目信息（表格形式，11 个字段）
   - 板块二：服务评估（包含 6 个模块）

3. **项目信息字段**
   - 业务归口、服务描述、服务类型、服务地点
   - 所属设备名称、所属设备型号、所属设备厂家
   - 服务设备数量、服务设备计量单位
   - 备注、需求详情
   - 空值显示为"-"或"无"

4. **服务评估模块**
   - 风险提示：表格形式，包含风险等级、名称、说明、建议措施、置信度
   - 施工任务：按任务分组，包含工种、职级、人数、工时
   - 施工人数及工时：汇总是否航修、总工时、总人数
   - 资源需求：需求工具、耗材、专用工具、设备/备件需求
   - 综述：置信度汇总、数据来源、学习资产参考
   - 审核重点：警告提示、重点关注

5. **是否航修判定**
   - 若 `service_location_type.name == "港口"`，则判定为航修

6. **设备/备件需求三栏**
   - `customer_provided`（客户提供）
   - `provider_provided`（我方提供）
   - `to_be_confirmed`（待确认）

7. **置信度标识**
   - 高：✓ 高
   - 中：⚠ 中
   - 低：⚡ 低

8. **风险等级标识**
   - 高：🔴 高
   - 中：🟡 中
   - 低：🟢 低

---

## Related Skills

- `parse-requirement-skill`: 需求解析与确认
- `search-history-cases-skill`: 历史案例检索
- `assessment-reasoning-skill`: 风险/工时/人数推理

---

## Feedback

- 有问题请继续补充样例
- 建议优先先固定 S6 输出 schema
- 后续可继续增强任务组织与备件归属逻辑